# Rezervis_backend

Start:
run MySQL using Xampp for e.g.
Create "rezervis" database and load sql data to MySQL
Open cloned Rezervis_backend folder, open cmd in that folder and run: composer install
Then run: php artisan serve

DB:
Download rezervis.sql, create new DB in MySQL and insert from file.
